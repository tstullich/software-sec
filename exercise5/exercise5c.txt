The least significant byte of the chunk size that was xmalloc()'d
We can only create chunks that end on the 00 byte, since the \0 character gets written into the least significant byte during strcpy()
The following chunk will have data from the overflown chunk "user data" which can be used to 
